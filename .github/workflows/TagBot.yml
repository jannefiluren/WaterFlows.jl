name: TagBot
on:
  issue_comment:
    types:
      - created
  workflow_dispatch:
    inputs:
      lookback:
        description: "[DEPRECATED] No longer has any effect"
        default: "3"
jobs:
  TagBot:
    if: github.event_name == 'workflow_dispatch' || github.actor == 'JuliaTagBot'
    # ubuntu-slim doesn't support containers, and thus won't work
    runs-on: ubuntu-latest
    steps:
      - uses: JuliaRegistries/TagBot@e4c81aa138ef4179bddea165bb4a9680c223aca4 # v1.25.9
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # NOTE: token-only setup. GITHUB_TOKEN cannot tag commits that modify
          # workflow files — those releases must be created manually. To restore
          # full automation, re-add an SSH deploy key with `workflow` scope:
          # ssh: ${{ secrets.DOCUMENTER_KEY }}